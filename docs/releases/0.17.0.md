# AI Project Studio 0.17.0

**Unsigned prerelease.** These installers are not production-qualified. Windows
may show an unknown-publisher or SmartScreen warning. The macOS build is unsigned
and not notarized; Gatekeeper may block it. Version 0.10.3 remains the stable
release.

**Upgrade note:** the entire `0.11.x` line is superseded and marked known-bad.
Upgrade past it.

This release ships the **Bug Bash** agent — a read-only, edge-case bug hunter
that runs a team of parallel testers across metasessions.

## What's new

- **Bug Bash agent** — attach it to any feature with a linked repository to hunt
  edge-case bugs. Describe the feature and its setup, and a **scenario analyst**
  reads the feature and repository to generate reviewable test scenarios. On
  approval, a **lead agent** splits them across a team of **tester** sub-agents
  that each run on their own metasession **in parallel**, then compiles a
  findings report.
  - Live **agent hierarchy** with per-agent time, tokens, and AI credits; click
    any agent for its live log; each tester lists the scenarios it owns.
  - **Read-only** — Bug Bash never edits code, cuts a branch, or opens a PR.
  - Background-safe: switching windows does not stop generation or a run, and an
    in-flight pass can be cancelled and reset (terminating attached processes).
    Accepted scenarios are preserved so you can re-run without regenerating.
  - Report renders as formatted markdown with pass/fail/blocked tallies and
    every scenario's verdict.

## Documentation

- New **[docs/features/bug-bash.md](../features/bug-bash.md)** — the Bug Bash
  user guide.
- **[docs/agents.md](../agents.md)**, `backend-modules.md`, and the feature
  README updated to list Bug Bash as a shipped agent and describe its engine.

## Reliability

- At the release commit the gated source builds cleanly and passes the backend
  and UI coverage gates at 100% line/branch/function/statement coverage.

## Known limitations

- Live GitHub and Azure DevOps operations, installed Copilot/Agency CLI
  processes, authenticated MCP child servers, and a real warm metasession under
  provider load still require an environment qualification run.
- Credits remain genuinely unknown for providers that do not report usage; the
  view shows the unknown state and the tokens it does have rather than inventing a
  cost.
- Windows and macOS installer upgrade/restore qualification is not complete.
  Signing is intentionally omitted from this prerelease; macOS cannot
  self-install updates until signing and notarization are available.

Back up important workspace data before trying this prerelease. Installer
provenance manifests are included and retain their unqualified status.
