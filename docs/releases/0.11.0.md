# AI Project Studio 0.11.0

**Unsigned prerelease.** These installers are not production-qualified. Windows may show an unknown-publisher or SmartScreen warning. The macOS build is unsigned and not notarized; Gatekeeper may block it. Version 0.10.3 remains the stable release. No automatic-update feed is published for this prerelease.

## Improvements

- A new animated desktop startup screen shows the HD app icon, live loading stages, and elapsed time in light or dark theme. The interface appears only after it loads and paints; startup errors remain visible, and closing during startup safely shuts down owned services.
- The startup design includes a subtle technical grid, illuminated corners, orbiting accents, and a light sweep, with reduced-motion support.
- Updated charting and packaging dependencies, and split shared frontend libraries into smaller bundles without increasing the bundle-size warning limit.
- Warm metasession settings preserve requested sizes and show live pool states, resizing progress, and actionable failures instead of appearing to revert changes.
- Settings > Diagnostics includes manual retained clipboard-image management, with explicit confirmation and warnings that deleting images can break existing or resumed prompts.
- Meta-operation results, usage, and attempt histories persist across restarts and are available through the API and UI.
- Session and automation deletion, cancellation, process cleanup, and shutdown have safer ownership tracking and retry behavior.
- Shared process and queue limits, bounded event streams, and bounded renderer caches improve resource control.
- Terminal delivery, Unicode handling, clipboard boundaries, keyboard navigation, and update guidance have been improved.

## Known limitations

- Full packaged/native, upgrade/restore, and supported-platform qualification is not complete. Signing is intentionally omitted from this prerelease.
- Automatic retained-image cleanup is unavailable; deletion is manual because provider-history references cannot be proven absent.
- Automatic installation remains withdrawn in favor of guided release-page installation.
- Long-duration soak runs were waived, not completed.

Back up important workspace data before trying this prerelease. Installer provenance manifests are included; their pending qualification status remains accurate after publication.
